# Simple BitCoin wallet creator

first, execute the function createWallet(test=true) for a test wallet <br>
or <br>
createWallet(test=false) for a wallet in the main bitcoin network for real use <br>

and thats it
